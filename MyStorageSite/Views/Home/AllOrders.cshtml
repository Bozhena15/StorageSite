@model IEnumerable<MyStorageSite.Models.Order>

@{
    ViewData["Title"] = "AllOrders";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<h2>AllOrders</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-bordered">
    <thead>
        <tr>
            @*<th>
            @Html.DisplayNameFor(model => model.User)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.contactPhone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderData)
        </th>
        <th></th>*@
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.contactPhone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderData)
            </td>*@
            <td>
                <a href="~/Order/Details/@item.Id" asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-dark">Details</a>
            </td>
        </tr>
}
    </tbody>
</table>
